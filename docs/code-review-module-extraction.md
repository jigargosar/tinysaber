Code Audit — Full Codebase (2026-02-22)

Reviewed: src/main.js, src/scene.js, src/sabers.js, src/blocks.js,
src/particles.js, src/hud.js, src/controllers.js, src/xrSession.js, src/music.js

Generated by /code-audit skill (2 parallel agents: standards + bugs)

## HIGH

### 1. SSOT — Color constants duplicated across 4 files
`main.js:22-23`, `sabers.js:42-43`, `blocks.js:25-26`, `particles.js:12-13`
Extract shared `src/colors.js`. → **keep**

### 2. ISI — `hitCtx` mutable side-channel between `onControllerFrame` and `onHit`
`main.js:82-103`
Pass `hand` directly to `onHit` via closure, delete `hitCtx`. → **keep**

### 3. Stale Reference — Module-level temporaries shared across HitTester instances
`blocks.js:34-40`
Move temporaries into `createHitTester` closure so each instance owns its own. → **keep**

## MEDIUM

### 4. ISI — Particle `active` flag redundant with array membership
`particles.js:23,33,37,57`
Replace with free-pool stack. → **keep**

### 5. Logic — `segmentHitsBox` double-expands with `CUBE_HIT_MARGIN`
`blocks.js:43,136`
Build `_box` from `(CUBE_SIZE + CUBE_HIT_MARGIN * 2) / 2`, remove `expandByScalar`. → **keep**

### 6. State Transitions — XR session button allows concurrent entry
`xrSession.js:6-32`
Add `sessionState` flag (`idle`/`starting`/`active`), guard at top. → **keep**

### 7. API Design — `onHit` receives shared pre-allocated `_hitPos` vector
`blocks.js:149-150`
Use `_blockPos.clone()` per hit (infrequent, not hot path). → **keep**

### 8. Magic Numbers — `sabers.js` unnamed geometry radii/segments
`sabers.js:15,20,27,33`
Name all: `HANDLE_RADIUS`, `BLADE_RADIUS`, `GLOW_RADIUS`, `GEO_SEGMENTS`, etc. → **keep**

### 9. Resource Leak — `setInterval` in music never cleared
`music.js:210-216`
Start interval in `start()`, clear in `stop()`. → **keep**

### 10. Naming — Comment says "even bars" but code fires on odd indices
`music.js:65-66`
Fix comment, name `INTRO_KICK_EXTRA_STEP = 2`. → **keep**

### 11. Logic — Ghost-hit sweep on first frame after session start
`blocks.js:158-161`
Reset `prev` to far-away position `(0, -100, 0)` instead of origin. → **keep**

### 12. TDA — `controllers` receives raw function refs from `blocks`
`main.js:45`, `controllers.js:5`
Group into `debugActions` object. → **uncertain** — low impact for two callbacks.

## LOW

### 13. Magic Numbers — `scene.js` unnamed layout/camera literals
`scene.js:15,21,33,37-43`
Name all wall/grid/camera constants. → **keep**

### 14. Least Privilege — `controllers` receives full `music` object
`main.js:45`
Pass only `music.toggle`. → **keep**

### 15. SSOT — `STEPS_PER_BAR` local to function, no pattern validation
`music.js:56`
Hoist to module level, validate stage pattern lengths. → **keep**

### 16. Magic Numbers — `LANES_Y` base and spacing unnamed
`blocks.js:9`
Name `LANE_Y_BASE`, `LANE_Y_SPACING`. → **keep**

### 17. Null — `getElementById` no null guard in `xrSession.js`
`xrSession.js:6,35`
Add null guard with thrown error. → **keep**

### 18. Resource Leak — `AudioContext` never closed
`music.js:189`
Add `destroy()` method. → **keep**

### 19. Logic — `getSongSection` unreachable fallback returns silently
`music.js:72`
Replace with `throw`. → **keep**

### 20. Abstraction — `createGraph` leaks `audioCtx`/`compressor` to player factories
`music.js:93-180`
→ **uncertain** — module-internal coupling, may be premature to fix.

## Summary

```
+=====================+========+
| Severity            | Count  |
+=====================+========+
| High                | 3      |
+---------------------+--------+
| Medium              | 9      |
+---------------------+--------+
| Low                 | 8      |
+---------------------+--------+

+=====================+========+
| Category            | Count  |
+=====================+========+
| Magic Numbers       | 4      |
+---------------------+--------+
| ISI / SSOT          | 3      |
+---------------------+--------+
| Logic Error         | 3      |
+---------------------+--------+
| API Design / TDA    | 3      |
+---------------------+--------+
| Resource Leak       | 3      |
+---------------------+--------+
| State Transitions   | 1      |
+---------------------+--------+
| Naming              | 1      |
+---------------------+--------+
| Null Guard          | 1      |
+---------------------+--------+
| Abstraction         | 1      |
+---------------------+--------+

Needs discussion: #12 (debugActions object), #20 (audio graph abstraction)
Agent convergence: 4 of 20 findings independently found by both agents
```
